# Apple Mail Fast MCP — Claude Code entry point

Read and follow [the shared project instructions](../AGENTS.md) before working.
They are canonical for both Claude Code and Codex, including the GitHub workflow,
architecture, validation commands, and links to the domain skills in this directory.

**Version:** v0.11.0

The version field remains here for the existing release workflow and version-sync
gate. Do not duplicate project guidance or derived test/line counts here.

Claude-specific hooks remain in `settings.json`. The `/merge-and-status` command
delegates to the shared post-merge procedure linked from AGENTS.md.
